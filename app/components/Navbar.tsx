import UnmountStudio from "./Unmount";

export default function Navbar() {
  return (
    <UnmountStudio>
      <header className="flex items-center justify-center text-center p-6 border-b border-zinc-800">
        Navbar Component
      </header>
    </UnmountStudio>
  );
}
